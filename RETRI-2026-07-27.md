# Re-tri des 88 MINEUR / ENTRETIEN — 2026-07-27

Complément de `REPRISE-2026-07-27.md` (qui re-triait les 25 BLOQUANT/MAJEUR).
Les 53 MINEUR + 35 ENTRETIEN de `AUDIT-2026-07-21.md` ont été vérifiés un par un
contre le code du 27/07 (`6e3d86f`) par 11 agents, un lot par sous-système, avec
preuve `fichier:ligne` courante exigée pour chaque verdict.

## Bilan

| Verdict | Nombre | Détail |
|---|---|---|
| CORRIGÉ | **15** | toutes des MINEUR |
| PÉRIMÉ | **1** | script supprimé |
| OUVERT | **72** | 38 MINEUR + 34 ENTRETIEN |
| INCERTAIN | 0 | |

⚠ La prédiction de la REPRISE (« vu le taux de 80 % déjà corrigé sur les sévérités
hautes, en attendre autant ») **ne tient pas** : les correctifs de la semaine ont
visé les bloquants/majeurs et le rendu. Le tout-venant mineur est resté en place
(taux réel : 18 % fermées).

Les numéros de ligne ci-dessous sont ceux du **code courant du 27/07**, pas ceux
de l'audit.

---

## CORRIGÉES — ne pas re-signaler (15)

| Audit | Preuve du correctif |
|---|---|
| cloudSave.js:88 (flush avant save final) | `main.js:783-789` : handler unique `save()` puis `cloudMirrorSave({force:true})` |
| HelpBubble.jsx:38 (bulle orpheline) | `helpBubbleCore.js:86-98` `tipStillAlive` + poll 250 ms + Échap |
| blackjack.js:104 (main traverse le GR) | `blackjack.js:418` `purgeBlackjackHand`, appelé à `building.js:372` |
| building.js:255 (Échap exhumation paie) | `building.js:267` : options[0] = « Renoncer », court-circuit si `!choice?.buildingId` |
| icarus.js:209 (vol survit au GR) | `icarus.js:124-128` `purgeIcarusFlight`, appelé à `building.js:371` |
| state.js:1697 (message ×4 Ragnarök) | `state.js:1900-1915` : `buildGrandResetState(nextCount, seals)` teste `seals.includes(11)` |
| building.js:322 (recordGrPerformed au rang) | `building.js:357-358` : boucle sur les numéros de sceau |
| state.js:1445 (save illisible écrasée) | `state.js:1606-1622` : backup `-corrupt-backup` + `console.error` |
| crisis.js:510 (chronique avant non-retour) | `events.js:91-101` `logCollapseLine` après `completeCollapse` ; verrouillé par `collapse.persistence.test.js` |
| main.js:200 (offline ignore gamePaused) | `main.js:481` : garde `gamePaused` en tête d'`applyOfflineProgress` |
| state.js:1307 (nextBoonAt sans borne) | `state.js:1462` : clamp à `Date.now() + BOON_INTERVAL_MAX_SEC` |
| cityMapRuntime.js:376 (hit-test merveilles) | `cityMapRuntime.js:558` : `wonderAnchor` partagé avec le peintre + `projection.test.js` |
| layout.js:1217 (cmEngineInstances sans id) | `layout.js:1263` : `cmEngineInstances(level, meta.id)` |
| isoRenderer.js:2112 (lumières orphelines) | halos déposés pendant la passe peintre sous `lampArt.ready` (`isoRenderer.js:5155-5166`) ; résidu limité au mode dégradé sans calque lumière |
| renderWorld.js:556 (quais reconstruits/frame) | quais **bakés** en iso (`isoRenderer.js:5824-5836`, passe `base`/`glow`) ; le legacy reste en direct, assumé |

## PÉRIMÉE (1)

- `scripts/makeOsselets.mjs` — supprimé par `0b02e24` (22/07). Le gabarit
  `bones.png` reste protégé par `.bones.stamp`.

---

## OUVERTES (72) — groupées par ce qu'elles coûtent

### A. Fausse la Faveur ou les règles du temple (8 MINEUR + 2 ENTRETIEN)

1. **augures.js:461-491** — quitte-ou-double : perte écrêtée au solde
   (`Math.max(0, faveur − wager)`), gain payé plein → EV positive si le solde
   passe sous le wager pendant l'animation (automatisations qui débitent).
2. **augures.js:476-489** — le même quitte-ou-double n'appelle aucun recorder de
   la Chronique : gains/pertes invisibles des stats à vie.
3. **icarus.js:251-260** — jalon GR VII + chronique du jackpot déclenchés sur la
   condition « pot > 0 », pas « rake > 0 » : décrochage sur une rafle de 0 Faveur.
4. **templePot.js:129-133** — `potRake` peut créditer une Faveur fractionnaire
   (share=1 + pot fractionnaire), seul chemin qui casse le contrat « entière ».
5. **scratch.js:246** — la relance payée par la cella (`potFunded`) est comptée
   comme mise du joueur dans `chronicleStats`.
6. **templeAutomation.js:268-285** — `unlockTempleAuto` débite sans
   `recordShopSpend`, contrairement aux deux autres kinds du même arbre.
7. **BlackjackStage.jsx:88-110** — reprise d'une main : `setChosenStake(null)`
   puis fallback `stakes[0]` → « Redistribuer » rejoue à la mise Légère ×1.
   (Deux bullets d'audit, un seul défaut, une seule correction.)
8. **templeAutomation.js:211-239** — `setTempleAuto` fait `save()` complet à
   chaque événement d'input du curseur cible / plancher de Faveur.
9. *(ENTRETIEN)* **ScratchStage.jsx:244-251** — `startTicket` écrase `pendingRef`
   sans flush préalable (défense en profondeur).
10. *(ENTRETIEN)* **templeGames.js:39-50** — « un seul jeu actif » garanti au
    niveau scène seulement, pas moteur.

### B. Simulation hors-ligne (4 MINEUR + 1 ENTRETIEN)

11. **tick.js:445** — la gazette (`checkAndTriggerChronicleEntries`) tourne sans
    garde `isNotifyPaused`/`isOfflineSim` pendant la sim ; les entrées persistées
    ne sont pas restaurées par le `finally` (qui ne rend que `state.history`).
12. **chronicleEvaluator.js:215** — `save()` inconditionnel à chaque publication ;
    le throttle nuage de 30 s est battu par l'horloge virtuelle monkeypatchée.
13. **tick.js:414 / mythTicks.js:194-210** — Cadmos en sim : `promptCadmosAgeName`
    non gardé pose `cadmosPromptPending` → tous les ticks suivants sortent avant
    `checkCrisisThresholds`, l'auto-effondrement rupture100 ne tire plus.
    (La garde ajoutée depuis vise le spam de modales en ligne, pas ce cas.)
14. **main.js:379-392** — chemin linéaire du retour d'absence : `rates()` évalué
    une fois à l'heure réelle → minutes restantes de Bénédiction/Braisiers
    créditées ×1. (La clepsydre et le chemin farm, eux, sont corrects.)
15. *(ENTRETIEN)* **crisis.js:708-709** — `togglePolicy` seul verbe sans garde
    `crisisLimitAnnounced` ; le GEL ne tient que par le verrou de navigation.

### C. Affichages qui mentent au joueur (5 MINEUR)

16. **olympus.js:85** — l'écran promet « grave un cran » par effondrement, le
    code ajoute `ferveur/100` : proclamation à ~13-16 chutes au lieu de 12.
17. **PrestigeView.jsx:51** — compte à rebours d'effondrement auto mort : teste
    l'upgrade supprimé `intendant_de_crise`.
18. **pressure.js:195** — le ×1,25 Théocratie absent de l'Anatomie de la
    Rupture : les lignes ne se réconcilient pas avec la cible.
19. **BuildingShop.jsx:311/361** — la ligne de production par rangée ignore
    `riverEngineFactor` et Babel exponentiel (le chip B6, lui, est juste).
20. **AuguresPanel.jsx:178** — infobulle périmée « Trois Soleils raflent la
    cagnotte » (la règle n'existe plus ; l'autre infobulle du fichier est juste).

### D. Electron, build & scripts (3 MINEUR + 4 ENTRETIEN)

21. **main.cjs:63-80** — fichier absent de dist/ : `net.fetch` rejette, fenêtre
    blanche muette (l'anti-traversal ajouté ne couvre pas ce cas). Clone frais
    sans `npm run build` = écran blanc sans message.
22. **preload.cjs:20-34** — sonde synchrone D:→Z: (46 `existsSync`) au
    démarrage ; un lecteur réseau mort gèle le lancement.
23. **quantize.cjs:20** — `SKIP_DIRS` n'exclut toujours ni `wonders/` ni
    `tree-base.png` : le mode lot écraserait les merveilles @32 et la fresque.
24. *(ENTRETIEN)* **package.json:21-25** — le .exe embarque `_archive`,
    `iso/_orig`, `buildings/_orig`, `Asepritelayers` (Vite copie `public/`
    intégralement ; vérifié présent dans `dist/`). Pèse sur les 32,6 Mo.
25. *(ENTRETIEN)* **main.cjs:46-47** — commentaire « OneDrive » périmé.
26. *(ENTRETIEN)* **scripts/_warmTile.mjs** — « TEMPORAIRE À SUPPRIMER » toujours
    là, écrit en place.
27. *(ENTRETIEN)* **scripts/isoToneDown.cjs:32** — réécrit en place, non
    idempotent (chaque relance compose la désaturation).

### E. Accessibilité & dialogues (7 MINEUR + 1 ENTRETIEN)

28. **ImportDialog.jsx:53** — `style={{display:'block'}}` inline neutralise le
    `display:none` natif (frame de flash hors modal).
29. **OptionsDialog.jsx:365-414** — onglets sans état ARIA, aria-label sur div
    sans rôle.
30. **ChronicleTicker.jsx:55-61** — dépêche `<div onClick>` sans rôle/tabIndex :
    inutilisable au clavier.
31. **OdometerNumber.jsx** — bandes de chiffres lues comme du charabia par le
    lecteur d'écran (aucun aria-hidden, pas de frère `.sr-only`).
32. **OutcomeFloatLayer.jsx:25** — région aria-live montée avec son contenu
    (même motif : bandeau d'ère App.jsx:467, ticker re-monté par key).
33. **StageHelp.jsx:71-73** — tooltip sans id/aria-describedby, pas d'Échap.
    (La moitié HelpBubble de ce bullet est corrigée, elle.)
34. **views-regulation.css:1150 + 1518** — Silkscreen en prose (bannière jackpot
    grattables, callouts augures) — infraction à la règle typo.
35. *(ENTRETIEN)* **DebugDialog.jsx:16-20** — clic sur le padding interne ferme
    le dialogue (pas le contrôle de coordonnées qu'a OptionsDialog).

### F. Interface & React (2 MINEUR + 3 ENTRETIEN)

36. **HeritageView.jsx:123** — `autoKey` sans `stakePow` : le cadran Coffre se
    met à jour avec ~1 s de retard (via le fragment instability).
37. *(ENTRETIEN)* **PurchaseRow.jsx:103/108** — setTimeout 900/400 ms sans
    cleanup → setState après démontage.
38. *(ENTRETIEN)* **RuinsTreePixel.jsx:214** — cleanup wheel n'enlève pas
    `rt-interacting`.
39. *(ENTRETIEN)* **useCountUp.js:16-47** — gel si `duration` change en pleine
    montée (latent : consommateurs actuels stables).

### G. Carte, agents & runtime (5 MINEUR + 2 ENTRETIEN)

40. **agents.js:1316-1337** — `updateVehicles` sans remap hors-réseau : véhicule
    gelé à jamais (les piétons ont le remap, eux).
41. **cityMapRuntime.js:1761-1779** — `captureFrame({citizens:'none'})` vide
    vehicles/ships sans les restaurer ; rebuild gated derrière un changement de
    sig. ⚠ Touche le harnais `__cityShot`.
42. **cityMapRuntime.js:1415-1419** — chaque `forceFrame`/`captureFrame` ajoute
    une boucle rAF concurrente permanente (`__demoCity` en fait ~17).
43. **renderWorld.js:2357-2370** — spawn d'émeute : `filter()` de tout
    `walkRoadList` par émeutier créé (hitch à la frame de déclenchement).
44. **isoRenderer.js:5043** — cull des items 'tile' par la cellule d'origine
    seule : champs 10×6 et emprises multi-tuiles popent au bord de l'écran
    (l'aqueduc a le bon test, ligne 5032).
45. *(ENTRETIEN)* **cityMapRuntime.js:1/2, 68 + layout.js:8/19** —
    imports morts masqués par `/* eslint-disable */`.
46. *(ENTRETIEN)* **cityMapRuntime.js:1425-1429** — branche `if (!CM.cw)
    resize()` inatteignable.

### H. Rendu & perf carte — backlog perf en partie assumé (2 MINEUR + 3 ENTRETIEN)

47. **isoRenderer.js:5716-5780** — la rafale de décodages caméra immobile paie
    toujours une recuisson pleine par frame : `ed96528` protège des gestes
    caméra, pas des invalidations `soft` à l'arrêt. Le scénario « cascade de
    gels au chargement d'une mégapole » reste possible. **Candidat perf n°1**
    (cousin direct de la passe bâtiments listée dans la REPRISE).
48. **engineSprites.js:157-166** — conteurs stade 0 : garde en `||`, tuile vide
    si seule la bande de feu est décodée.
49. **renderBuildings.js:373-386** — échec fetch/parse du JSON de flammes avalé
    (`catch(()=>{})`), sentinel null définitif, pas de `r.ok`.
50. *(ENTRETIEN)* **isoRenderer.js:1878-1891** — ruban du fleuve reprojeté
    plusieurs fois par frame (atténué par 4d09e3c, pas éliminé).
51. *(ENTRETIEN)* **isoRenderer.js:2853/3965 + renderWorld.js:1574/2605/2643** —
    gradients recréés par frame (sillages, halos de torche, reflets legacy).
52. *(ENTRETIEN)* **isoRenderer.js:5230 + 3826** — `vehicleLaneOffset` appelé 2×
    par véhicule/frame, objet alloué par appel.

### I. État & multi-onglets (1 MINEUR + 1 ENTRETIEN)

53. **state.js (deux onglets)** — toujours aucun listener `storage` /
    BroadcastChannel / Web Locks. Atténué : le .exe est protégé par
    `requestSingleInstanceLock` ; le risque ne concerne que le navigateur.
54. *(ENTRETIEN)* **state.js:33-34** — doc d'une migration v5 inexistante
    (`CURRENT_SAVE_VERSION` vaut 4).

### J. Code mort & docs périmées (14 ENTRETIEN)

55. **prestige.js:193 + balance.js:804** — `grandResetMythsRequired` /
    `MYTH_GATE_START_GR` morts depuis les sceaux en ordre libre (2 bullets, un
    seul nettoyage).
56. **pressure.js:245-260** — quatre planchers `Math.max` inatteignables.
57. **pressure.js:168 & co** — ~8 canaux d'effet de ruines lus sans `effectType`
    déclaré (le compte a baissé : 3 ont été câblés depuis) ; la puce
    « ruines +N% » de PressureAnatomy reste inatteignable.
58. **FaveurShop.jsx:16** — démonté volontaire documenté, mais les 13 règles
    `.faveur-shop*` de views-regulation.css restent livrées.
59. **templeArtifacts.js:30-32** — `noyePotMult()` mort, logique dupliquée
    inline dans templePot.js:23.
60. **balance.js:262** — `AUGURY_DOG_CLEMENCY_CRANS` câblé nulle part, valeur 2
    en dur dans crisis-cost.js:42.
61. **balance.js:433 + 559** — `WING_BASCULE_LEVEL` / `GRAVEUR_BASCULE_LEVEL`
    non consommés, borne RTP<1 en commentaire seulement.
62. **upgrades.js:19/35, branchTheme.js:9, 4 verbes close*** — 7 exports morts.
63. **CityView.jsx:237-238** — commentaire « prix VIVANT » faux (lot figé).
64. **myths.js:726** — `orPopPeak` écrit jamais lu + commentaires du plafond
    d'Âge d'Or disparus (rates.js:61, mythEffects.js:71-74).
65. **myths.js:146/149/169** — branches `pending` mortes.
66. **data/myths.js:531/553** — `buildChoiceHTML`/`readChoice` sans appelant.
67. **data/myths.js:55-56 vs :75** — doc cooldown Arche : 2 min écrits, 100 s
    réels.
68. **cityEngineSprites.js:225** — `caravan-oxcart-classical` chargé jamais
    blitté.
69. **cityEngineSprites.js:312-328** — `propPivot` duplique le scan alpha de
    `propBBox`, sans `willReadFrequently`.
70. **renderBuildings.js:329/940** — `drawMinimap` mort (exporté, importé,
    jamais appelé).

*(Numérotation de commodité : 70 entrées pour 72 bullets, deux paires d'audit
fusionnées — BlackjackStage :106/:125 et prestige.js:193/balance.js:781.)*

---

## Suggestion d'ordre d'attaque

1. **Lot A** (temple : Faveur/règles) — c'est la même famille que M3/M4 de la
   REPRISE ; un seul chantier « équité du temple » peut fermer A + M3 + M4.
2. **Lot B + C** (hors-ligne + affichages menteurs) — visibles par le joueur.
3. **Lot D** (Electron) — surtout #21/#24 avant la sortie de version 0.0.0
   (point 3 du « Reste à faire » de la REPRISE).
4. **Lot E** (a11y) — mécanique, groupable en une passe.
5. **Lots G/H perf** — à mesurer d'abord (leçon de la REPRISE : le profileur,
   pas la lecture) ; #47 est le seul avec un scénario de gel identifié.
6. **Lots F/I/J** — entretien, par opportunité.
